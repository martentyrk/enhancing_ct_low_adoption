[DEFAULT]
p0 = 0.001
p1 = 0.03
alpha = 0.001  # False negative rate
beta = 0.01  # False positive rate
prob_g = 0.99
prob_h = 0.10

policy_weight_01 = 0.0
policy_weight_02 = 0.0
policy_weight_03 = 0.0

# Differential privacy
dp_method = 5  # if negative, no DP is applied, see constants.py for options
epsilon_dp = 1.0  # if negative, then no DP is applied
delta_dp = 0.001
a_rdp = -42  # if negative, then no RDP is applied (dp_method 5 and up)
clip_lower = -1.  # if negative, no clipping is applied
clip_upper = 10000.  # if >1, no clipping is applied

# Noisy tests
# Options are: -1=unchanged; 0=almost zero; 1=low; 2=medium; 3=high
noisy_test = 1

num_days_window = 14  # Default to large number
quantization = 128

# Number of rounds for inference
num_rounds = 5